on: 
   create:
    branches:   
      - '*'
      - '!master'
    tags:
      - '!fake'
jobs:
  BuildCandidate:
   runs-on: ubuntu-latest    
   steps:
    - uses: actions/checkout@v3
      with:
        persist-credentials: false
